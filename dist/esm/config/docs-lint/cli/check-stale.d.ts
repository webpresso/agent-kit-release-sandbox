#!/usr/bin/env bun
/**
 * Stale Document Detection
 *
 * Finds documentation files with last_updated older than MAX_AGE_DAYS.
 * Exits with code 1 if stale docs are found (fails CI).
 */
export {};
//# sourceMappingURL=check-stale.d.ts.map